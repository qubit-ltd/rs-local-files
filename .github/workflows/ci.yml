name: Rust CI

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write
  pages: write
  id-token: write

jobs:
  rust-ci:
    uses: qubit-ltd/rs-ci/.github/workflows/rust-ci.yml@14ec4b152a19f8be585730f6ee945bc5ff822ad8
    with:
      run_windows_tests: true
      run_macos_tests: true
      coverage_enforce_thresholds: "1"

  unix-cross-check:
    name: Compile-only ${{ matrix.target }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        target:
          - x86_64-unknown-freebsd
          - armv7-linux-androideabi

    steps:
      - name: Checkout source
        uses: actions/checkout@v6
        with:
          fetch-depth: 1
          submodules: recursive

      - name: Install Rust target
        run: rustup toolchain install 1.94 --profile minimal --target ${{ matrix.target }}

      - name: Compile-only check target
        run: cargo +1.94 check --target ${{ matrix.target }}

  test-discovery:
    name: Integration test discovery
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v6
        with:
          fetch-depth: 1
          submodules: recursive

      - name: Install Rust
        run: rustup toolchain install 1.94 --profile minimal

      - name: Run nested integration tests
        run: >-
          cargo +1.94 test --test lib_tests
          options::local_copy_source_mode_tests::test_local_copy_source_modes_are_distinct
          -- --exact
